# NAMFX Context

智能电吉他综合效果器平台：PC（JUCE 独立应用 + VST3/AU 插件）与嵌入式设备（RK 系 Cortex-A Linux 主力档 / STM32H7 低端档）共享同一纯 C++17 音频引擎（core/）。

## Language

**预设**:
一条完整的音色链配置（JSON 文件，含 chain + 场景），是可分享的最小持久化单元。
_Avoid_: 音色, patch, program

**场景**:
预设内 8 个持久化状态槽之一，记录"链上哪些模块开关 + 参数组合"；场景只改开关与参数，不改链拓扑，可命名（≤8 字符）。
_Avoid_: 快照, 演出场景

**A/B**:
两个临时对比缓冲，存当前链状态供前后对比；不持久化、不入 undo 栈、不随预设保存。
_Avoid_: 快照, 场景

**单块**:
用户面对链上模块的分类称呼（pedal）；类别分单块 / 箱头 / 箱体。
_Avoid_: 模块（用户面禁用；引擎面专用）

**模块**:
引擎面（代码/JSON）对链上处理单元的称呼；以模块ID 为全局唯一键，类别（pedal|amp|cab）是模块的属性。
_Avoid_: 单块, 效果器

**箱头**:
放大器建模模块（含前级+后级），引擎类别 amp。
_Avoid_: 音箱, 功放

**箱体**:
扬声器建模模块，引擎类别 cab；实现为 IR 卷积或 DSP。
_Avoid_: 喇叭

**音色链**:
预设内按序排列的槽位序列（≥8，可扩展），吉他信号从左到右逐槽处理。
_Avoid_: 信号链（通用语；本平台用音色链）

**槽位**:
音色链上的一个位置，承载一个模块（含 bypass/mix）。
_Avoid_: 坑位, 位置

**旁路**:
用户主动对单模块或整链的开关操作，带交叉淡化防爆音。
_Avoid_: 直通, mute

**干音直通**:
系统在预设/场景切换期间的 audition 策略：新链后台预加载时旧链继续响，切换瞬间干音淡入；仅用于预设切换，场景切换不走此路径。
_Avoid_: 旁路, 静音

**演出模式**:
整机行为模式：锁定编辑、脚踩/编码器驱动预设与场景切换、切换无爆音。
_Avoid_: 演出场景, live mode（代码可用 live_mode）

**手动模式**:
演出模式下的子行为：脚踩直控链上模块的开关。
_Avoid_: 编辑模式（编辑模式是演出模式的反面，另立词）

**现场状态记忆**:
变更后节流保存的现场状态（链+参数+当前场景指针，最后一次编辑后 2-5s 写盘 + 原子写入协议），掉电后开机回到该状态；独立于场景槽，不静默覆盖任何场景。
_Avoid_: 自动快照, 记忆

**性能档位**:
平台能力分级（Full / Limited / Minimal），声明允许加载的 NAM 模型架构、最大 IR 长度、最大混响尺寸；档位由运行时实测校准。
_Avoid_: 档位（单独使用易与 MIDI channel 混淆）

**控制源**:
可改变参数值的输入来源统一抽象（归一化 0..1）：表情踏板 / MIDI CC / Web UI / 桌面 UI / 脚踩开关；含绑定语义（源→参数）与多源仲裁。
_Avoid_: 输入源, controller

**引擎**:
core/ 的音频处理核心，纯 C++17 零运行时依赖；双端共用，桌面端与嵌入式都是薄壳。
_Avoid_: 核心（目录叫 core 但引擎是概念名）, engine

**模型库**:
.NAM 模型与 IR WAV 资产的自动扫描索引（目录扫描 + 文件监听热更新）。
_Avoid_: 音色库, 预设库

**干/湿**:
dry = 未经链处理的输入信号；wet = 经链处理后的信号；录音总线 dry tap 在输入增益后、wet tap 在 master 后全局 EQ 前。
_Avoid_: 原声/效果声（口语可用但文档统一干/湿）

**过载/失真/模糊**:
三类驱动效果（overdrive/distortion/fuzz）：过载 = 软削波（保留动态）；失真 = 硬削波（压缩动态）；模糊 = 非对称削波（方波质感）。
_Avoid_: 互相混用

**切换策略**:
预设切换（G12：干音直通 + 预加载 + 50-100ms 淡入）与场景切换（即时参数应用 ≤10ms 交叉淡化）两种音频过渡路径的总称。
_Avoid_: 渐入渐出（只描述淡入细节）
